const arr = [1, 2, 3, 4];

// will print 4
console.log(arr);
console.log(arr.length);

// what will happen if we explicitly modify the size of an array?
arr.length = 10;
console.log(arr);
console.log(arr.length);